name: Versioning Automation with SemVer

on:
  push:
    branches: 
      - master

jobs:
  Workflow-calling:
    uses: tiagomartimiano/github-automated-semver-workflow/.github/workflows/versioning.yml@v1.0.0
    secrets: inherit